node_modules/
**/node_modules/
.keys/
**/.keys/
.env
**/.env
.env.local
*.local
*.log
.DS_Store
**/.DS_Store
dist/
build/
.claude/

# Vendored c2pa-web bundle + WASM (regenerate with `cd verify && npm run vendor`)
verify/vendor/

# Private key files — never commit these
*.pem
*.p12
*.pfx
*.key

# Exception: allow .env.example (template only, no real values)
!.env.example
